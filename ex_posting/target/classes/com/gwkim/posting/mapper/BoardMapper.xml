<?xml version="1.0" encoding="UTF-8"?>
<mapper>
	<select id = "getListWithPagin" resultType = "com.gwkim.posting.domain.BoardVO">
	<![CDATA[
	select * from
	(
	select /* INDEX_DESC(tbl_board pk_board) */ rownum rn, bno, title, writer, regDate, updateDate
	from tbl_board
	where bno > 0 and rownum >0 and rownum <= (#{pageNum}*#{amount})
	)
	where rn > ((#{pageNunm}-1)*#{amount})
	]]>
	</select>
</mapper>